# Titan Package B

This is a sub-package of the `titan` namespace.

## Description

`package_b` is part of the `titan-workspace` monorepo example.

## Usage

This package provides the `package_b` command.

```bash
uv run package_b
```
