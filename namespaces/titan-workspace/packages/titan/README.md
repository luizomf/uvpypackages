# Titan Core Package

This is a sub-package of the `titan` namespace.

## Description

`titan` is part of the `titan-workspace` monorepo example.

## Usage

This package provides the `titan` command.

```bash
uv run titan
```
