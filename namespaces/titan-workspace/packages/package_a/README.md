# Titan Package A

This is a sub-package of the `titan` namespace.

## Description

`package_a` is part of the `titan-workspace` monorepo example.

## Usage

This package provides the `package_a` command.

```bash
uv run package_a
```
